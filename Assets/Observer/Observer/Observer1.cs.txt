using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Observer1 : MonoBehaviour, OnNotified
{

     [SerializeField] LifeSubstracter _lifeSubstracter;
     [SerializeField] Events _event;
    // Start is called before the first frame update
    void Start()
    {
        _lifeSubstracter=FindObjectOfType<LifeSubstracter>();
        _event = FindObjectOfType<Events>();

       
    }

    

    public void Notified()
    {
        print ("Observer1 Notified");
        
            _event.KilledEvent();

       
        // si la vida del player es <= 0
        //invoco al metodo Killed;
    }
}
