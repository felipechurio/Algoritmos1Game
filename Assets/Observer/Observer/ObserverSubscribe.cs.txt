using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObserverSubscribe : MonoBehaviour, ObserverSubscriber, OnNotified

{

    [SerializeField] Observer1 observer1;
   void Start()
    {
        ObserverSubscribes();
    }
   

    public void ObserverSubscribes()
    {

        Observado.Instance.ObserversAmount.Add(observer1);
        print("Suscrito a Observado");
    }
    public void Notified()
    {}

    
}
